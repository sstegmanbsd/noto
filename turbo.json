{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/*"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
